- content_for(:title, t('.title'))
h1
  = t('.title')

div
  ul
    li
      = link_to '受注中', '#'
    li
      = link_to '達成済', '#'
  div
    - if @theme_boards.present?
      = render @theme_boards
    - else
      p
        = t('.no_result_achievement')